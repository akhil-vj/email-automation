[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/build"

# Environment variables for production build
[build.environment]
  REACT_APP_API_URL = "https://email-automation-8lva.onrender.com"
